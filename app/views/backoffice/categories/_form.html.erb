<%= form_for [:backoffice, @category], url: {action: action} do |f| %>
  <div>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  
  <div>
    <%= f.label :cover %>
    <%= f.file_field :cover %>
  </div>
  
  <div>
    <%= f.submit t(submit) %>
  </div>
<% end %>